This repository contains project modules. The current module is:

- [`data_pipeline/`](data_pipeline/README.md) — scrapes books.toscrape.com,
  cleans/types the data, converts currency, loads a normalized SQLite
  database, and runs SQL queries with a pandas/SQL equivalence check.
  See `data_pipeline/README.md` for install/run instructions and design
  decisions.
